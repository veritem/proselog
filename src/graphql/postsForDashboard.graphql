query postsForDashboard($domainOrSubdomain: String!, $includeDrafts: Boolean) {
  site(domainOrSubdomain: $domainOrSubdomain) {
    id
    posts(includeDrafts: $includeDrafts) {
      nodes {
        id
        title
        content
        publishedAt
        published
      }
    }
  }
}
