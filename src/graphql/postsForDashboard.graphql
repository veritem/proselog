query postsForDashboard(
  $domainOrSubdomain: String!
  $visibility: PostVisibility
) {
  site(domainOrSubdomain: $domainOrSubdomain) {
    id
    posts(visibility: $visibility) {
      nodes {
        id
        title
        content
        publishedAt
        published
      }
    }
  }
}
