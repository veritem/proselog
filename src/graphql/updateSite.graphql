mutation updateSite(
  $id: String!
  $name: String
  $subdomain: String
  $description: String
) {
  updateSite(
    id: $id
    name: $name
    subdomain: $subdomain
    description: $description
  ) {
    id
  }
}
