mutation updateSite(
  $id: String!
  $name: String
  $subdomain: String
  $introduction: String
) {
  updateSite(
    id: $id
    name: $name
    subdomain: $subdomain
    introduction: $introduction
  ) {
    id
  }
}
