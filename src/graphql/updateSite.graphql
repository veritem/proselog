mutation updateSite(
  $id: String!
  $name: String
  $subdomain: String
  $description: String
  $icon: String
) {
  updateSite(
    id: $id
    name: $name
    subdomain: $subdomain
    description: $description
    icon: $icon
  ) {
    id
  }
}
