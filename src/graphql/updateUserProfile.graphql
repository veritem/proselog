mutation updateUserProfile(
  $userId: String!
  $name: String
  $username: String
  $email: String
  $avatar: String
) {
  updateUserProfile(
    userId: $userId
    name: $name
    username: $username
    email: $email
    avatar: $avatar
  ) {
    id
  }
}
