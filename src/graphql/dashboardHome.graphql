query dashboardHome($domainOrSubdomain: String!) {
  site(domainOrSubdomain: $domainOrSubdomain) {
    id
    name
    subdomain
    stats {
      id
      postCount
      subscriberCount
    }
  }
}
